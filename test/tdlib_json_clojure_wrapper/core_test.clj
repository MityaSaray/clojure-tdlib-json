(ns tdlib-json-clojure-wrapper.core-test
  (:require [clojure.test :refer :all]
            [tdlib-json-clojure-wrapper.example :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
